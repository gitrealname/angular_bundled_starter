import angular from 'angular';
/* 
* Register all components here. Example:
import About from './about/about';
*/

export default angular.module('<%= bundleName %>.components', [
/*
* Include all registered components. Example:  
  About.name,
*/  
]);
